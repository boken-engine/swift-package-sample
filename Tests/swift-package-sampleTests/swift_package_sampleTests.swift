import XCTest
@testable import swift_package_sample

final class swift_package_sampleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_package_sample().text, "Hello, World!")
    }
}
